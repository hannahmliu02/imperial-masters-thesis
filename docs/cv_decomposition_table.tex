% Per-pair projection variability at the selected layer, decomposed.
% s_i = <delta_i, d_hat> is the signed per-pair projection onto the unit demographic
% direction; mean and sd are normalised by the residual norm ||h|| so they are
% comparable across models. CV = sd(s_i)/|mean(s_i)| is scale-free.
\begin{table}[htbp]
  \centering
  \caption{Per-résumé consistency of the injected demographic direction at each
    model's selected layer. The coefficient of variation (CV) is decomposed into its
    normalised mean and normalised standard deviation ($\div\lVert h\rVert$). Qwen's
    low CV is driven primarily by a much tighter spread ($\mathrm{sd}$), not by a
    larger mean: Mistral-7B's normalised spread is $6$--$20\times$ that of the Qwen
    rungs. Values are means over seeds ($n{=}5$ per Qwen rung; $n{=}15$ Mistral).}
  \label{tab:cv_decomposition}
  \begin{tabular}{lcccc}
    \toprule
    Model & Layers & Norm.\ mean $\lVert v\rVert/\lVert h\rVert$
          & Norm.\ sd $\mathrm{sd}(s_i)/\lVert h\rVert$
          & CV $\mathrm{sd}(s_i)/|\overline{s}|$ \\
    \midrule
    Qwen2.5-0.5B & 24 & 0.810 & 0.068 & 0.09 \\
    Qwen2.5-3B   & 36 & 0.578 & 0.021 & 0.04 \\
    Qwen2.5-7B   & 28 & 0.556 & 0.071 & 0.13 \\
    Qwen2.5-14B  & 48 & 0.554 & 0.071 & 0.13 \\
    \midrule
    Mistral-7B   & 32 & 0.350 & 0.447 & 1.29 \\
    \bottomrule
  \end{tabular}
\end{table}
