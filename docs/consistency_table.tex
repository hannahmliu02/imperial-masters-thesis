% Per-résumé consistency of the injected demographic direction at each model's
% selected layer, via three independent measures: directional alignment (mean per-pair
% cosine with the aggregate direction, and its angle), sign agreement (fraction of pairs
% agreeing in sign), and CV (sd/|mean| of the per-pair projection strength). All three
% agree that the injection is far more résumé-uniform in Qwen than in Mistral.
% Means over seeds (n=5 Qwen rungs, n=15 Mistral).
\begin{table}[t]
  \centering
  \small
  \caption{Per-résumé consistency of the injected direction at the selected layer.
    Three independent measures agree: individual résumé pairs align tightly with the
    aggregate bias direction in Qwen (small angle, high sign agreement, low CV) but
    only weakly in Mistral-7B, whose pairs sit near-orthogonal to the mean ($69^\circ$)
    with an effect-strength spread exceeding its mean (CV${>}1$). Cosine is the mean
    per-pair cosine with the aggregate direction; angle is its $\arccos$; sign
    agreement is the fraction of pairs sharing the majority sign; CV${=}\,\mathrm{sd}(s_i)/|\overline{s}|$.}
  \label{tab:consistency}
  \begin{tabular}{lrccc}
    \toprule
    Model & Sel.\ layer & Alignment (cos / angle) & Sign agree. & CV \\
    \midrule
    Qwen2.5-0.5B & 23     & 0.82 \;/\; $35^\circ$ & 1.00 & 0.09 \\
    Qwen2.5-3B   & 31--32 & 0.98 \;/\; $11^\circ$ & 1.00 & 0.04 \\
    Qwen2.5-7B   & 18--27 & 0.78 \;/\; $39^\circ$ & 1.00 & 0.13 \\
    Qwen2.5-14B  & 47     & 0.64 \;/\; $51^\circ$ & 1.00 & 0.13 \\
    \midrule
    Mistral-7B   & 29--31 & 0.36 \;/\; $69^\circ$ & 0.97 & 1.29 \\
    \bottomrule
  \end{tabular}
\end{table}
