% Compact summary of the per-layer CV tables (docs/qwen_cv_perlayer_tables.tex).
% CV = sd(s_i)/|mean(s_i)| of the per-pair projection. It is high (noisy) in early
% layers and collapses at the selected deep layer. The selected-layer CV is decomposed
% into its normalised mean and sd (both divided by ||h|| so they are comparable across
% models), showing the low Qwen CV is driven by a much tighter spread, not a larger
% mean. Means over seeds (n=5 Qwen, n=15 Mistral).
\begin{table}[t]
  \centering
  \small
  \caption{Projection-variability (CV) collapse toward the selected layer, decomposed.
    CV is high in the early layers and drops sharply at the identified layer. The
    selected-layer CV is split into its normalised mean $\lVert v\rVert/\lVert h\rVert$
    and normalised spread $\mathrm{sd}(s_i)/\lVert h\rVert$: every Qwen rung reaches a CV
    an order of magnitude below Mistral-7B's floor, and this is driven by the spread
    (Mistral's is $6$--$20\times$ larger), not the mean. \emph{CV early} averages the
    first half of the layers.}
  \label{tab:cv_summary}
  \begin{tabular}{lrrrrrr}
    \toprule
    & & Selected & CV & \multicolumn{3}{c}{At selected layer} \\
    \cmidrule(lr){5-7}
    Model & Layers & layer & (early)
          & mean $\tfrac{\lVert v\rVert}{\lVert h\rVert}$
          & sd $\tfrac{\mathrm{sd}(s_i)}{\lVert h\rVert}$
          & CV \\
    \midrule
    Qwen2.5-0.5B & 24 & 23     & 2.04 & 0.810 & 0.068 & 0.09 \\
    Qwen2.5-3B   & 36 & 31--32 & 1.53 & 0.578 & 0.021 & 0.04 \\
    Qwen2.5-7B   & 28 & 18--27 & 1.22 & 0.556 & 0.071 & 0.13 \\
    Qwen2.5-14B  & 48 & 47     & 1.20 & 0.554 & 0.071 & 0.13 \\
    \midrule
    Mistral-7B   & 32 & 29--31 & 3.04 & 0.350 & 0.447 & 1.29 \\
    \bottomrule
  \end{tabular}
\end{table}
